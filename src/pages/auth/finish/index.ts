import AuthFinished from "@/pages/auth/finish/page.tsx";
import { currentRoute } from "@/pages/auth/finish/model.ts";

export const AuthFinishedRoute = {
  view: AuthFinished,
  route: currentRoute,
};
